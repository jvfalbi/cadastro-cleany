<style>
  .os-print-area { display: none; }

  @media print {
    @page { size: A4 portrait; margin: 5mm 7mm; }
    html, body { background: #fff !important; color: #111; margin: 0; padding: 0; font-family: system-ui, -apple-system, 'Segoe UI', sans-serif; }
    .sidebar, .footer, .no-print, .mobile-topbar, .app-shell > .layout > .sidebar,
    .page-header, .card, .print-logo { display: none !important; }
    .app-shell, .layout, .layout--nav-drawer { display: block !important; }
    .main-content, .container { padding: 0 !important; margin: 0 !important; max-width: 100% !important; }
    .os-print-area { display: block !important; }

  /* Cada via = 1 página, bloco compacto (~1/3 da folha) no topo */
    .os-via {
      border: 1.2px solid #222;
      border-radius: 3px;
      padding: 3.5mm 4mm 3mm;
      margin: 0;
      position: relative;
      box-sizing: border-box;
      width: 100%;
      max-width: 100%;
      page-break-inside: avoid;
      break-inside: avoid;
      page-break-after: always;
      break-after: page;
    }

    .os-via:last-child {
      page-break-after: auto;
      break-after: auto;
    }

    .os-via-label {
      position: absolute;
      top: 0;
      right: 0;
      background: #1b4332;
      color: #fff;
      font-weight: 800;
      font-size: 8px;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      padding: 2px 10px 2.5px;
      border-radius: 0 3px 0 6px;
    }

    .os-via-label--gerente { background: #1b4332; }
    .os-via-label--funcionario { background: #2d6a4f; }
    .os-via-label--cliente { background: #40916c; }

    .os-header {
      display: flex;
      align-items: center;
      gap: 2.5mm;
      margin-bottom: 1.5mm;
      padding-bottom: 1.5mm;
      padding-right: 20mm;
      border-bottom: 1.2px solid #222;
    }
    .os-header-logo img { max-height: 28px; width: auto; display: block; }
    .os-header-info { flex: 1; min-width: 0; }
    .os-header-empresa { font-size: 10.5px; font-weight: 700; line-height: 1.2; color: #1b4332; }
    .os-header-sub { font-size: 7px; color: #555; margin-top: 0.3mm; }
    .os-header-num { text-align: right; flex-shrink: 0; }
    .os-header-num-big { font-size: 15px; font-weight: 800; line-height: 1.1; color: #111; }
    .os-header-num-label { font-size: 6.5px; text-transform: uppercase; letter-spacing: 0.08em; color: #666; }
    .os-header-status {
      display: inline-block;
      font-size: 7px;
      font-weight: 700;
      text-transform: uppercase;
      padding: 1px 6px;
      border: 1px solid #333;
      border-radius: 3px;
      margin-top: 1.5px;
    }
    .os-emitted { font-size: 6.5px; color: #666; margin-top: 1mm; }

    .os-section { margin-bottom: 1mm; }
    .os-section-title {
      font-size: 6.5px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      color: #1b4332;
      background: #eee;
      padding: 1px 3px;
      margin-bottom: 1px;
      border-left: 2px solid #2d6a4f;
    }

    .os-client-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1.5mm 4mm;
    }
    .os-field-label {
      font-size: 6px;
      text-transform: uppercase;
      letter-spacing: 0.04em;
      color: #666;
      line-height: 1.2;
    }
    .os-field-value {
      font-size: 8.5px;
      font-weight: 600;
      word-break: break-word;
      line-height: 1.25;
      color: #111;
    }
    .os-field-value--name { font-size: 9.5px; font-weight: 700; line-height: 1.25; }
    .os-field-value--fantasia { font-size: 7.5px; font-weight: 500; color: #333; margin-top: 0.2mm; }
    .os-field--span2 { grid-column: 1 / -1; }

    .os-address-box {
      margin-top: 1mm;
      padding: 1.5mm 2mm;
      background: #f5f5f5;
      border: 1px solid #ccc;
      border-radius: 2px;
    }
    .os-address-box .os-field-label { font-weight: 700; color: #333; margin-bottom: 0.5mm; }
    .os-address-line { font-size: 8px; font-weight: 500; line-height: 1.3; color: #111; }
    .os-address-line + .os-address-line { margin-top: 0.3mm; }

    .os-service-row {
      display: flex;
      flex-wrap: wrap;
      gap: 2mm 5mm;
      margin-bottom: 1.5mm;
    }
    .os-desc {
      font-size: 8px;
      line-height: 1.35;
      white-space: pre-line;
      word-break: break-word;
      min-height: 5mm;
      padding: 1px 0;
    }

    .os-signatures {
      display: flex;
      justify-content: space-between;
      gap: 8mm;
      margin-top: 2mm;
    }
    .os-sig-block { flex: 1; text-align: center; }
    .os-sig-line {
      border-top: 0.8px solid #444;
      margin-top: 8mm;
      padding-top: 1px;
      font-size: 6.5px;
      color: #555;
    }

    .os-cut-line {
      display: none !important;
    }
  }
</style>
